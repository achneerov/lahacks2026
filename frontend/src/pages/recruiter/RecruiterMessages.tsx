import PagePlaceholder from '../../components/PagePlaceholder';

export default function RecruiterMessages() {
  return (
    <PagePlaceholder
      eyebrow="Recruiter"
      title="Messages"
      description="Talk to applicants about your open roles."
      issueNumber={12}
    />
  );
}
